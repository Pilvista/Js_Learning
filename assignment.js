// Function name: kilometerToMeter converion result return koro
//budgetCalculator 3 perameter will take like watch, capacitor, battery
//
function kilometerToMeter(kilometer){
    meter = 1000 * kilometer;
    return meter;
}
function budgetCalculator(watch, capacitor,battery){
    var watch_price = 100;
    var capacitor_price = 2;
    var battery_price = 10;
    var total_price = watch*watch_price + capacitor *capacitor_price + battery*battery_price;
    return total_price;
}
function hotelCost(days){
    var count_days = 0;
    if(days <= 10){
        count_days = days;
        var firstcost = days * 100;
    else if ( days <= 20){
        cost = (days - firstcount) * 80;


    }

    }
}

